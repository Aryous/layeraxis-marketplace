# outline.md 格式规范

拆图完成后最先输出 `imgs-spec/outline.md`，作为全局规划索引。

## 格式

```markdown
拆图方案：共 N 张

图 1：[概念焦点]
  主要目的：[解释流程/横向对比/揭示原理/情感渲染等]

图 2：...
...
```

## 原则

- outline 只做索引，不展开深度思考和评分
- 详细的功能层定位、三维评分、场景设计等全部跟着每张图的独立 `NN-*.md` 文件走
- `density` 变更时 outline + 所有 NN-*.md 一起作废重建
